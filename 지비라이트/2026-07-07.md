# 2026-07-07 개발 기록

## 지비라이트 작업 기준 정리

지비라이트 프로젝트의 후속 작업을 시작하기 전에 기준 경로와 기록 위치를 정리했다.

### 결정 사항

- 테이블 구조와 컬럼 정보는 `C:\Users\USER\Documents\Codex\shared\table-catalog`의 테이블 정보를 기준으로 확인한다.
- 모든 업무 기록은 `C:\Users\USER\Desktop\WorkLog` 아래 프로젝트명 폴더에 보관한다.
- 지비라이트 프로젝트의 기록 폴더는 `C:\Users\USER\Desktop\WorkLog\지비라이트`이다.
- 일일 공개 기록은 기존 규칙대로 `YYYY-MM-DD.md` 파일에 작성한다.

### 확인 내용

- `C:\Users\USER\Desktop\WorkLog\지비라이트` 폴더가 존재함을 확인했다.
- 기존 기록 파일 `2026-07-06.md`, `2026-07-06-internal.txt`, `README.md`를 확인했다.
- 전일 기록 기준으로 다음 구현 우선순위는 CT TVP 타입 확인, CT 저장 프로시저 연결, CT TEMP MERGE, 코팅 부모/중복 검증, 신규 저장 반환값 반영, 전사 영역 구현이다.

### 다음 작업

- 실제 소스가 연결되면 `table-catalog` 기준으로 CT 테이블/TVP 컬럼을 먼저 대조한다.
- 구현 결과와 결정 사항은 이 WorkLog 지비라이트 폴더에 계속 기록한다.

## 작업 종료 정리

- 종료 시각: 2026-07-07 18:03:45 +09:00

### 수행 작업

- 제조표준서 신규 저장과 작성중 저장 흐름을 검토했다.
- 문서 버전관리 공통 프로세스를 정리하고, 이후 작업에서 반복 참조할 기준 문서와 참조 위치를 정리했다.
- 제조표준서의 기본정보, 파트, 수지, 코팅, 전사 영역이 함께 저장되는 흐름을 점검했다.
- 신규 저장 시 문서관리용 헤더 정보를 함께 생성하는 방향을 검토했다.
- 그리드 데이터가 없을 때 발생하는 저장 전 데이터 변환 오류의 원인을 확인하고 수정 방향을 정리했다.
- 코팅 팝업 조회 결과가 두 번째 그리드에 잘못 바인딩되는 원인을 찾아 수정 방향을 확인했다.
- 코팅 작업 조건의 화면 구조와 제조표준서 저장 구조가 맞지 않는 문제를 확인했고, 저장 구조를 조정하는 방향으로 정리했다.

### 결과

- 제조표준서 저장 구조의 큰 흐름과 문서 개정 프로세스 기준을 정리했다.
- 수지, 코팅, 전사 그리드 저장을 위한 부모/자식 데이터 구성 방식을 정리했다.
- 저장 실패 원인이 단순 데이터 중복이 아니라 화면 바인딩과 저장 구조 불일치에서 발생했음을 확인했다.
- 다음 작업에서는 코팅 작업 조건 저장 구조를 먼저 확정한 뒤 저장 프로시저와 화면 데이터 생성부를 함께 맞추면 된다.

### 다음 작업

- 코팅 작업 조건 저장 구조 확정
- 저장 프로시저의 코팅 영역 반영
- 화면에서 생성하는 저장용 데이터 구조 재검증
- 신규 저장 테스트 재수행
- 이후 확정 및 개정 처리 흐름 구현

## 작업 재개

- 재개 시각: 2026-07-07 08:35:01 +09:00
- 현재 Codex 작업공간 `C:\Users\USER\Documents\Codex\2026-07-07\20`에서 지비라이트 작업을 시작했다.
- 전일 기록과 공용 테이블 카탈로그를 확인했고, 다음 구현 우선순위는 CT TVP 타입 확인, CT 저장 프로시저 연결, CT TEMP MERGE, 코팅 부모/중복 검증이다.
- 현재 작업공간에는 실제 C#/SQL 소스가 없어 소스 연결 전까지는 기준 정리와 작업 대기 상태로 둔다.
